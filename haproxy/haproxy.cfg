global
	#log /dev/log	local0
	#log /dev/log	local1 notice
	#chroot /var/lib/haproxy
	#stats socket /run/haproxy/admin.sock mode 660 level admin
	#stats timeout 30s
	#user haproxy
	#group haproxy
	daemon

	# Default SSL material locations
	#ca-base /etc/ssl/certs
	#crt-base /etc/ssl/private

	# Default ciphers to use on SSL-enabled listening sockets.
	# For more information, see ciphers(1SSL). This list is from:
	#  https://hynek.me/articles/hardening-your-web-servers-ssl-ciphers/
	#ssl-default-bind-ciphers ECDH+AESGCM:DH+AESGCM:ECDH+AES256:DH+AES256:ECDH+AES128:DH+AES:ECDH+3DES:DH+3DES:RSA+AESGCM:RSA+AES:RSA+3DES:!aNULL:!MD5:!DSS
	#ssl-default-bind-options no-sslv3

defaults
	log	global
	mode	http
	option	httplog
	option	dontlognull
        timeout connect 5000
        timeout client  50000
        timeout server  50000
	errorfile 200 /etc/haproxy/errors/200.http
	#errorfile 400 /etc/haproxy/errors/400.http
	#errorfile 403 /etc/haproxy/errors/403.http
	#errorfile 408 /etc/haproxy/errors/408.http
	#errorfile 500 /etc/haproxy/errors/500.http
	#errorfile 502 /etc/haproxy/errors/502.http
	#errorfile 503 /etc/haproxy/errors/503.http
	#errorfile 504 /etc/haproxy/errors/504.http

frontend ddp
	bind :2222
	acl healthcheck path_reg ^.*/healthcheck$
	use_backend healthcheck_backend if healthcheck

backend healthcheck_backend
	mode http
	http-request set-log-level silent
	errorfile 503 /etc/haproxy/errors/200.http

frontend cdap_service_frontend
	bind :11015
	acl is_get method GET
	acl is_post method POST
	acl is_delete method DELETE
	acl is_put method PUT
	acl truncate path_reg /truncate$
	acl server path_reg ^/v3/namespaces/[a-zA-Z0-9\-\_]+/streams
	use_backend cdap_backend    if server is_post !truncate !is_put !is_delete !is_get

backend cdap_backend
	#dev
	server cdap01 10.17.3.69:11015
	#prod
	#server cdap01 10.18.3.138
